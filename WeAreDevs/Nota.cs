namespace WeAreDevs
{
    /*
     * => 1x1:
     * propriedade de acesso em 1 objeto (T)
     * 
     * => 1xN:
     * propriedade de acesso nos 2 objetos (T) (ICollection<T>)
     * 
     * => NxN:
     * propriedade de acesso nos 2 objetos (ICollection<T>) (ICollection<T>)
     * tabela auxiliar: id de ambas as tabelas + propriedade de acesso
    */
}